/*package it.example.petrisnetsgame;

import android.content.Context;
import android.graphics.Canvas;
import android.graphics.Color;
import android.graphics.Paint;
import android.graphics.Rect;
import android.view.View;


public class MyCanvas extends View {

    Paint paintState, paintToken;

// al posto di context mipmap
    public MyCanvas(Context context) {
        super(context);
    }


    @Override
    protected void onDraw(Canvas canvas) {
        super.onDraw(canvas);

        paintToken = new Paint();
        paintToken.setColor(Color.BLACK);
        paintToken.setStrokeWidth(10);
        paintToken.setStyle(Paint.Style.FILL);

        paintState = new Paint();
        paintState.setColor(Color.BLACK);
        paintState.setStrokeWidth(10);
        paintState.setStyle(Paint.Style.STROKE);


        canvas.drawCircle(500,1000,40, paintState);
        canvas.drawCircle(500,1000,20, paintToken);

        canvas.drawCircle(500,500,40, paintState);
        canvas.drawCircle(700,800,40, paintState);

    }
}

 */
